<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    /**
     * Seed the application's database.
     *
     * pTable has no baseline data to seed — every table is created through
     * the app itself (there is deliberately no users/admins table; see
     * requirement.md §9).
     */
    public function run(): void
    {
        //
    }
}
